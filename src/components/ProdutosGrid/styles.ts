import styled from "styled-components"

export const GridContainer = styled.div`
    width: 940px;
    height: 600px;

    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    align-items: center;
    gap: 20px;
`